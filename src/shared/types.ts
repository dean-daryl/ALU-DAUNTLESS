export interface BlogType {
  title: string;
  cover: string;
  description: string;
}
export interface ChallengeType {
  date: string;
  name: string;
  grade: string;
}
export interface MemberType {
  picture: string;
  name: string;
  description: string;
}